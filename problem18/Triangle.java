package problem18;

/*public class Triangle {

	private TriangleNode[] triangle;
	private int numRows;
	
	public Triangle(int n, int[] nums) {
		triangle = new TriangleNode[nums.length];
		numRows = n;
		int curr = 0;
		
		for(int i=0; i<nums.length; i++) {
			triangle[i] = new TriangleNode(curr, i, j, nums[curr], false);
			curr++;
		}
	}
	
	public String toString() {
		StringBuilder s = new StringBuilder();
		
		for(int i=0; i<numRows; i++) {
			s.append("  ");
			for(int j=0; j<triangle[i][j].getRow(); j++) {
				
			}
		}
		
		return s.toString();
	}
}
*/